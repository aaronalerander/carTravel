services:
  - type: web
    name: car-travel
    runtime: node
    nodeVersion: 22
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: ./node_modules/.bin/playwright install chromium && npm start
    envVars:
      - key: NODE_ENV
        value: production
